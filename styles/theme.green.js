const colors = require('tailwindcss/colors')

const theme = {
  // Primary color
  alpha: colors.green,
  // Secondary color
  beta: colors.emerald,
  // Accent color
  accent: colors.lime,
  // Neutral color
  omega: colors.slate,
}

module.exports = theme
