const colors = require("tailwindcss/colors");

const theme = {
  // Primary color
  alpha: colors.pink,
  // Secondary color
  beta: colors.red,
  // Accent color
  accent: colors.yellow,
  // Neutral color
  omega: colors.silver,
};

module.exports = theme;
