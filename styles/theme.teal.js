const colors = require('tailwindcss/colors')

const theme = {
  // Primary color
  alpha: colors.teal,
  // Secondary color
  beta: colors.fuchsia,
  // Accent color
  accent: colors.pink,
  // Neutral color
  omega: colors.zinc,
}

module.exports = theme
